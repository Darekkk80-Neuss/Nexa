{
  "name": "effyra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check-build": "node check-build.mjs",
    "hooks": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
