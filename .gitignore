.claude/
.commitmsg.tmp

# Rechtstexte bleiben bewusst lokal (nicht ins öffentliche Repo)
legal/

# Build-Tooling (nicht deployen)
node_modules/

# Lokale Test-Datei (index.html ohne Anon-Key, nur zum lokalen Testen)
__test.html
